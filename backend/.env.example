# Puerto en el que corre la aplicación
PORT=3000 # Cambia esto si tu aplicación corre en otro puerto
# Puedes cambiar el puerto según tus necesidades
# Asegúrate de que este puerto esté libre y no sea utilizado por otra aplicación

# Configuración de la base de datos MySQL
DB_HOST=localhost # Cambia esto si tu base de datos está en otro host
DB_PORT=3306 # Puerto por defecto de MySQL
DB_USER=usuario # Cambia esto por tu usuario de MySQL
DB_PASSWORD=contraseña  # Cambia esto por tu contraseña de MySQL
DB_NAME=nombre_base_de_datos # Cambia esto por el nombre de tu base de datos

# Configuración de CORS y Entorno
NODE_ENV=production # development | production

# URLs permitidas para CORS (separadas por comas, SIN espacios)
# DESARROLLO: Déjalo vacío para localhost automático
# PRODUCCIÓN: ALLOWED_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com
ALLOWED_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com

# ⚠️  SEGURIDAD CRÍTICA: Clave secreta para JWT 
# DEBE ser de AL MENOS 32 caracteres, preferiblemente 64+
# NUNCA uses claves simples como 'holamundo', '123456', etc.
# Genera una clave segura: openssl rand -base64 32
# Ejemplo seguro: JWT_SECRET=AbCdEf123456XyZ789MnOpQrStUvWxYz012345AbCdEf67890
JWT_SECRET=tu_clave_secreta_muy_larga_y_segura_de_al_menos_32_caracteres

#configuración de nodemailer
EMAIL_USER=tu_correo@gmail.com # Cambia esto por tu correo electrónico
EMAIL_PASS=tu_contraseña # Cambia esto por tu contraseña de correo electrónico o una contraseña de aplicación si usas autenticación de dos factores