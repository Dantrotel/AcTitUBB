# Configuración para ambiente de testing
NODE_ENV=test
PORT=3000

# Base de datos de prueba (usar la misma BD de desarrollo para tests)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=actitubb
DB_PORT=3306

# JWT
JWT_SECRET=test_secret_key_for_testing_only
JWT_EXPIRES_IN=4h
JWT_REFRESH_EXPIRES_IN=7d

# API
API_URL=http://localhost:3000/api/v1

# Correo (deshabilitado en tests)
EMAIL_ENABLED=false
EMAIL_HOST=smtp.test.com
EMAIL_PORT=587
EMAIL_USER=test@test.com
EMAIL_PASSWORD=test

# Redis (opcional en tests)
REDIS_ENABLED=false
REDIS_HOST=localhost
REDIS_PORT=6379
